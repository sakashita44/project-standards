# 改行コード設定
# リポジトリ内は LF で統一する（クロスプラットフォーム対応）

# デフォルト: テキストファイルは LF で正規化する
* text=auto eol=lf

# Windows 固有スクリプト（CRLF が必要）
*.bat text eol=crlf
*.cmd text eol=crlf

# PowerShell（LF でも動作するが，Windows 環境を考慮して CRLF にする）
*.ps1 text eol=crlf
